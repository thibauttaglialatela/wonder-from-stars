{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Home{% endblock %}

{% block body %}
    <div class="w-full m-0 p-0 bg-cover bg-bottom"
         style="background-image:{{ asset('unsplash.jpg') }}; height: 60vh; max-height:460px;">
        <div class="container max-w-5xl mx-auto pt-16 md:pt-32 text-center break-normal">
            <!--Title-->
            <h1 class="text-white font-extrabold text-2xl md:text-7xl w-full">
                🔭 Wonders from the sky 🔭
            </h1>
        </div>
    </div>
    <!--Container-->
    <div class="container px-4 md:px-0 max-w-6xl mx-auto -mt-32">
        <div class="mx-0 sm:mx-6">
            <div class="bg-gray-200 w-full text-xl md:text-2xl text-gray-800 leading-normal rounded-t mb-5">
                <!--Lead Card image from Pexel-->
                <div class="flex flex-col md:flex-row h-full bg-white rounded overflow-hidden shadow-lg">
                    <div class="md:w-2/3">
                        {% if image.copyright is defined %}
                            <img src="{{ asset('build/galaxy-red-and-blue.jpg')|imagine_filter('image_compressed') }}"
                                 alt="galaxie rouge et bleue"
                                 class="h-full w-full shadow">
                        {% else %}
                            <img src="{{ image.url }}" alt="{{ image.title }}" class="h-full w-full shadow">
                        {% endif %}
                    </div>
                    <div class="md:w-1/3">
                        <div class="bg-white rounded-t md:rounded-b-none overflow-hidden shadow-lg pt-6 px-6">
                            {% if image.copyright is defined %}
                                <h2 class="w-full font-bold text-xl text-gray-900 px-6">Un ciel étoilé</h2>
                                <p class="text-gray-800 font-serif text-base">
                                    Sous le dôme noir de la nuit, un spectacle céleste extraordinaire se dévoile.
                                    Des milliers d'étoiles scintillent tels des diamants éparpillés sur un velours
                                    sombre. La Voie lactée, cette voie de lumière diffuse et mystérieuse, s'étire telle
                                    une écharpe d'argent à travers le firmament. Chaque étoile est une histoire en soi,
                                    une lueur venue de millions d'années-lumière pour caresser nos yeux émerveillés.

                                    Le ciel étoilé est un rappel silencieux de la beauté infinie qui nous entoure et de
                                    notre place au sein de l'univers. Il nous invite à lever les yeux de nos
                                    préoccupations terrestres pour contempler l'infini, à méditer sur notre existence
                                    éphémère dans le flux éternel du temps. Les étoiles brillent, non seulement comme
                                    des astres lointains, mais aussi comme des points de lumière dans nos cœurs, nous
                                    rappelant que même dans l'obscurité, il y a toujours de la beauté et de la brillance
                                    à découvrir.
                                </p>
                            {% else %}
                                <h2 class="w-full font-bold text-xl text-gray-900 px-6">{{ image.title }}</h2>
                                <p class="text-gray-800 font-serif text-base px-6 mb-5">{{ image.explanation }}</p>
                            {% endif %}
                        </div>

                        <div class="bg-white rounded-b md:rounded-t-none overflow-hidden shadow-lg p-6">
                            <div class="flex items-center justify-between">
                                {% if image.copyright is defined %}
                                    <p class="text-gray-600 text-base md:text-sm">Photo de Suzy Hazelwood</p>
                                {% else %}
                                    <p class="text-gray-600 text-base md:text-sm">Published
                                        the: {{ image.date|date("d/m/Y") }}</p>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </div>
                <!--/Lead Card-->
            </div>
        </div>
    </div>


{% endblock %}
